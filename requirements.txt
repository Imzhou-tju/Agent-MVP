# Clawgent 核心依赖
# 安装：pip install -r requirements.txt

# CLI 交互
typer>=0.9.0
questionary>=2.0.0
rich>=13.0.0
prompt_toolkit>=3.0.0

# 环境配置
python-dotenv>=1.0.0

# LangChain 生态
langchain-core>=0.2.0
langgraph>=0.1.0
langchain>=0.2.0
langchain-openai>=0.1.0

# LLM 提供商 SDK
openai>=1.0.0
anthropic>=0.18.0

# 数据存储
aiosqlite>=0.19.0
langgraph-checkpoint-sqlite>=3.0.0

# 工具支持
pydantic>=2.0.0

# 学术检索 MCP 接入（原生 MCP 客户端）
langchain-mcp-adapters>=0.1.0
# 学术 MCP server 用 uvx 按需拉起（需安装 uv：https://docs.astral.sh/uv/）
# 常用：arxiv-mcp-server / semantic-scholar-mcp / pubmed-mcp-server

# PDF 解析（MinerU 后端走官方 API，仅需 requests；PyPDF2 为默认/降级）
requests>=2.31.0
